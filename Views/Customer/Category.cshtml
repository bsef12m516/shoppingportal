
@{
    ViewBag.Title = "Check";
}


<html>
    <body>
        <h2>Check</h2>
@{

foreach (var i in Model)
        {
    string s= "/Content/pics/"+i.Pic;
            @*<div class="col-md-4">*@
                <div class="item">
                    <img src="@s" alt="@i.Description" style="width:200px;height:120px">
                </div>
                <div>
                    <p class="navbar-text">Rs @i.Price | Stock: @i.Stock</p>
                </div>
                </div>
        }
}
    </body>
    </html>
